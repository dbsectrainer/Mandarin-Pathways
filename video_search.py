"""Deprecated: Mandarin Pathways no longer bundles YouTube video IDs.

Embedded YouTube was removed from the web PWA and Flutter app.
This script is kept as a no-op stub so references to ``video_search.py`` fail gracefully.
Restore prior implementation from git history if you need the YouTube Data API helpers again.
"""


def main():
    print(
        "video_search.py: Embedded YouTube support was removed; this script does nothing."
    )


if __name__ == "__main__":
    main()
